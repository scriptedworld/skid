# FR-7.4, what is x

| ID | Question | |
|---|---|---|
| FR-7.4 | What is `x` in FR-3.2? | [?] |

Open. Carries no test until it closes.

FR-3.4 says the window is configurable with its default unset, so this is the
default rather than the only value it can take.

`clank/tasks/skid/greeting/10-how-long-the-quiet-window-is.questions`
