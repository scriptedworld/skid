# FR-7.6, is Python forced by kokoro

| ID | Question | |
|---|---|---|
| FR-7.6 | Is Python forced by kokoro, or chosen? kokoro has ONNX exports callable from other languages, so the model and the implementation language may be separable. Recorded because the first pass is Python by decision either way. | [?] |

Open. Carries no test until it closes.

FR-5.2 names the Python MCP SDK, which is a second reason for Python and not the
same reason, so an answer of "forced" and an answer of "chosen" both leave the
first pass where it is.

`clank/tasks/skid/language/10-is-python-forced-by-kokoro.questions`
