# FR-3.4, the window is configurable

| ID | Requirement | |
|---|---|---|
| FR-3.4 | `x` is configurable. Its default is 30 seconds. | [A] |

The default is FR-7.4, which also fixes what the window is
measured from.

Configurable by which route is not stated. FR-6.1 and FR-6.2 give the voice both
an MCP tool and the config file, and nothing says the window gets the same pair.
If it does, FR-7.1 already settles which one is the record.
